apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: worker
  namespace: grid
spec:
  replicas: 16
  template:
    metadata:
      labels:
        k8s-app: worker
    spec:
      containers:
      - name: worker
        image: gcr.io/stack-build/buildkube/grid/worker:latest
        args:
        - bot
        - --remote=http://server.grid.svc.cluster.local:8980
        - --parent=rbe_ubuntu1604
        - host-tools
